import 'package:flutter/cupertino.dart';

class Randomclass extends StatelessWidget {
  const Randomclass({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Messages"),
    );
  }
}
